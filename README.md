# Automatic-File-Organizer
This Python script efficiently organizes files in a specified directory by sorting them into subfolders based on their file extensions. It automatically creates new subfolders as needed.
